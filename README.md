# 🚀 WappManager API

A modular FastAPI-based backend providing REST and WebSocket APIs for authentication and real-time event handling.

---

## 🧩 Features

- ⚡ **FastAPI** — high-performance Python web framework  
- 🔑 **JWT Authentication** — secure access token generation and validation  
- 🧠 **MongoDB Integration** — via `motor` or `pymongo`  
- 🧾 **Structured Logging** — configurable through Python’s logging module  
- 🌐 **Versioned Routes** — all routes are namespaced under `/api/v1`  
- 🔄 **WebSocket Support** — for real-time event updates  
- 🧱 **Pydantic Models** — schema validation and JSON-safe serialization  

---

## 🏗️ Project Structure

